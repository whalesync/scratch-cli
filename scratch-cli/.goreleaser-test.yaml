# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: scratchmd

builds:
  # Test build - points to test/staging server
  - id: scratchmd-test
    main: ./cmd/scratchmd
    binary: scratchmd
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/whalesync/scratch-cli/internal/cmd.version={{.Version}}
      - -X github.com/whalesync/scratch-cli/internal/cmd.commit={{.Commit}}
      - -X github.com/whalesync/scratch-cli/internal/cmd.buildDate={{.Date}}
      - -X github.com/whalesync/scratch-cli/internal/api.DefaultScratchServerURL=https://test-api.scratch.md

archives:
  # Test archives - with "-test" suffix
  - id: scratchmd-test
    ids:
      - scratchmd-test
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}-test_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  disable: true

release:
  github:
    owner: whalesync
    name: scratch-cli
  draft: false
  prerelease: true
  name_template: "{{.ProjectName}} v{{.Version}}"
  # Do not upload source code archives

brews:
  - name: scratchmd
    ids:
      - scratchmd-test
    repository:
      owner: whalesync
      name: homebrew-scratch-cli-test
    # Use the release with the binaries
    url_template: "https://github.com/whalesync/scratch-cli/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    commit_author:
      name: whalesync-bot
      email: ci@whalesync.com
    homepage: "https://github.com/whalesync/scratch-cli"
    description: "Sync local Markdown files with your CMS (Webflow, WordPress, etc). (TEST RELEASE)"
    test: |
      system "#{bin}/scratchmd --version"

scoops:
  - repository:
      owner: whalesync
      name: scratch-cli-bucket-test
    ids:
      - scratchmd-test
    commit_author:
      name: whalesync-bot
      email: ci@whalesync.com
    homepage: "https://github.com/whalesync/scratch-cli"
    description: "Sync local Markdown files with your CMS. (TEST RELEASE)"
    license: "Proprietary"

source:
  enabled: false
