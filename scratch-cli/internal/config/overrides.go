package config

// This file is a placeholder to trigger re-compilation if needed,
// though the actual Overrides struct is injected into account.go via the previous tool call.
// Wait, I put the struct IN account.go.
// However, I need to make sure the root cmd imports config and uses the fields.
