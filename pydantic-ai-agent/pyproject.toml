[project]
name = "pydantic-ai-agent"
version = "0.1.0"
description = "Agent server bridging user, Scratch data, and LLMs"
requires-python = ">=3.10"

[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 88
