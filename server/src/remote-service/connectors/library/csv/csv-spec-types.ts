/* eslint-disable @typescript-eslint/no-empty-object-type */

export type CsvTableSpecExtras = {
  // Add properties here and they are saved in the DB and passed back to the connector.
};

export type CsvColumnSpecExtras = {
  // Add properties here and they are saved in the DB and passed back to the connector.
};
