// Re-export from shared-types for backwards compatibility
export type {
  ActiveRecordSqlFilter,
  SnapshotColumnContexts,
  SnapshotColumnSettings,
  SnapshotColumnSettingsMap,
} from '@spinner/shared-types';
